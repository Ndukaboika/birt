# Extract Guessing Parameter Estimates (c)

Returns guessing parameter estimates from a 3PL fit.

## Usage

``` r
guessing_params(object, prob = 0.95)
```

## Arguments

- object:

  A fitted 3PL model.

- prob:

  Credible interval width. Default 0.95.

## Value

A data frame with one row per item.

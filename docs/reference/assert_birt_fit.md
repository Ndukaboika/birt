# Check if object is a birt model fit (internal)

Check if object is a birt model fit (internal)

## Usage

``` r
assert_birt_fit(object)
```

## Arguments

- object:

  Any object.

## Value

TRUE if it's a birt fit, otherwise throws an error.
